name: Deploy Presentation to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'presentation.html'
      - 'docs/**'
      - '.github/workflows/presentation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Sync presentation to docs/index.html
        run: |
          mkdir -p docs
          cp presentation.html docs/index.html
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git diff --staged --quiet || git commit -m "Sync presentation.html to docs/index.html" && git push
